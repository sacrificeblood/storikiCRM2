{
  "name": "adboard-server",
  "version": "1.0.0",
  "description": "Minon Devils — traffic operations command center with shared Postgres storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5"
  }
}
